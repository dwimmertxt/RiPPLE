use std::fs::File;
use std::io::{Write, Error, stdout};



pub fn file(file: &Vec<String>) -> Result<Vec<Vec<u32>>, Error> {
    let mut frames: Vec<Vec<u32>> = Vec::new();
    Ok(frames)
}


pub fn pipe() -> Result<Vec<Vec<u32>>, Error> {
    let mut frames: Vec<Vec<u32>> = Vec::new();
    Ok(frames)
}